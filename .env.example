# Required for day-31 analysis (extractor + synthesizer)
ANTHROPIC_API_KEY=

# Optional: watchdog will POST failure alerts here if set; otherwise logs to stdout
SLACK_WEBHOOK_URL=

# Optional: run collector headed for debugging (default: headless)
HEADLESS=1
